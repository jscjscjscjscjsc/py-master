# 复制成 deploy/.env 再填
# 域名（解析到本机公网 IP，Caddy 会自动签 HTTPS 证书）
DOMAIN=pymaster.example.com

# 访问口令：BASIC_HASH 用下面这条命令生成，别把明文写进来
#   docker run --rm caddy:2 caddy hash-password --plaintext '你的密码'
BASIC_USER=teacher
BASIC_HASH=$2a$14$替换成caddy生成的哈希
