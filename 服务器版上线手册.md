# PyMaster 服务器版 · 上线手册

面向：把 PyMaster 挂到一台公网服务器上，让十几个人（学生/同事）通过 IP 直接访问。
本文以**阿里云 ECS · Windows Server 2022 · 公网 IP 8.152.3.17** 为例，别的云厂商同理。

---

## 一、先明确两件事

**1）数据库选型：SQLite 够用，不要上 MySQL。**

这个规模下 SQLite 不是"凑合"，是正解：

| 考虑点 | 本项目的实际情况 |
|---|---|
| 并发量 | 十几个人、每人点几下 —— 峰值每秒几个请求，SQLite 的处理能力是这个量级的几百倍 |
| 部署成本 | SQLite 是文件，零安装零配置；MySQL 要装服务、设账号密码、开端口、做备份脚本 |
| 备份 | 拷 `data\` 目录就是完整备份；MySQL 要 mysqldump（还得记得跑） |
| 写入安全 | 已开 WAL 模式 + 原子写，断电不容易坏 |
| 什么时候该换 | 上百人同时在线、或者要做多机部署/读写分离时再考虑。真到那天，数据都在标准 SQL 里，迁移不难 |

账号数据（`data/users.json`）**刻意没有搬进数据库**：它被平台几十处代码读写，
搬迁的收益远小于风险，而且保持"整个平台就是一个文件夹、拷贝即迁移"这个特性
对交付很重要。访问日志（只追加、要筛选分页统计）才放进 `data/access.db`。

**2）你的机器配置足够。** 2 核 2G 跑十几个人没问题。
唯一要注意的是**服务器上没有装任何东西也没关系** —— 分发包自带 Python 和全部依赖。

---

## 二、上传与启动（三步）

### 第 1 步：上传解压

把 `PyMaster-服务器版-YYYYMMDD.zip`（约 460MB）上传到服务器，解压到**路径短、不带中文空格**的目录，例如：

```
D:\PyMaster
```

> 用阿里云控制台的「远程连接 → 发送文件」，或 RDP 里直接拖拽，或用 `scp`。
> 解压后目录里应该能看到 `app.py`、`一键上线.bat`、`上线前自检.bat`、`runtime\`、`vendor\`。

### 第 2 步：放行端口（两处都要做，漏一个就连不上）

**① 阿里云安全组**（这一步最容易漏，漏了表现是浏览器一直转圈直到超时）

阿里云控制台 → 云服务器 ECS → 网络与安全 → **安全组** → 配置规则 → 入方向 → 手动添加：

| 项 | 值 |
|---|---|
| 协议类型 | 自定义 TCP |
| 端口范围 | `5000/5000` |
| 授权对象 | `0.0.0.0/0` |
| 备注 | PyMaster |

**② Windows 防火墙**：`一键上线.bat` 会自动加规则。
如果它提示权限不足，就右键那个 bat 选「以管理员身份运行」。

### 第 3 步：双击「一键上线.bat」

它会依次做四件事：生成 `.env` 配置 → 放行防火墙 → 离线安装依赖 → 启动服务。
首次约 1 分钟（解压随包 Python + 从本地 wheel 装依赖），**全程不需要联网**。

看到这几行就算成功：

```
[PyMaster] 服务器模式（多人公网）· 监听 0.0.0.0:5000
[PyMaster] 在线运行代码：关闭（公网默认）
```

> 上线前想先体检：双击「**上线前自检.bat**」。它会逐项检查文件完整性、
> 配置、依赖、数据目录可写性、端口占用，并把必须处理的问题标成 `[x]`。

---

## 三、上线后第一件事（务必做）

浏览器打开：

```
http://8.152.3.17:5000/admin
```

默认管理员 **admin / admin888**。这个口令在文档里是公开的，谁都能用它进后台，
所以**登录后立刻去「系统设置 → 管理员口令」改掉**。

改完还需要配一样东西：

**「系统设置 → 平台模型」** —— 粘贴你的大模型 API（Base URL + 模型名 + Key），
点「测试连接」确认可用后保存。

这一个配置决定了两种学生的体验：

- **没绑自己 Key 的学生**：直接用你这份平台 Key（适合小班、你自己掏额度）；
- **绑了自己 Key 的学生**：走他自己的额度，不花你的（适合开放注册）。

> 你在本机用的 opencode go 配置可以直接照抄：
> Base URL `https://opencode.ai/zen/go/v1`，模型 `deepseek-v4.1-flash`。
> 注意这条通道要求带浏览器 UA 和 `x-opencode-session` 头 —— 平台代码里已经默认带上，不用你操心。
>
> 不配平台模型也能跑：那样只有绑了自己 Key 的人能用 AI 功能，
> 但课程正文、练习、刷题、修行阁全部照常（判题在服务器模式下默认关闭，见下）。

---

## 四、学生怎么用

把 `http://8.152.3.17:5000` 发给他们即可。

1. 打开链接 → 注册账号（用户名 + 密码，不要邮箱验证）；
2. 进「模型配置」页 → 填自己的大模型 API → 「测试连接」→ 保存；
3. 开始学习。**不填也能进**，课程和练习照常看。

每个学生的 Key 只存在他自己账号记录里（`data/users.json` 的 `ai` 字段），
别人看不到，后台也只显示末四位（`****xkSc`）。

---

## 五、你能在后台看到什么

| 页面 | 内容 |
|---|---|
| **总览** | 注册总数、今日新增/活跃/登录、已绑模型人数、近 7 天登录失败、7 天活跃度图、最近关键动作 |
| **注册用户** | 每个人的注册时间、最后活跃、最近 IP、登录次数、绑的哪个模型、学习进度。点一行开详情抽屉：用过的全部 IP、登录明细、学习进度、模型绑定时间和来源 IP，还能替学生解绑模型/清空进度 |
| **访问日志** | 谁在什么时候、从哪个 IP、用什么设备做了什么。可按账号/IP/事件/日期筛选，可分页，可导出 CSV（Excel 直接打开不乱码） |
| **白名单** | 留空＝开放注册（推荐）。填了名单就只有名单里的人能注册和登录，可设过期日期和每日 AI 上限 |
| **系统设置** | 平台模型、安全沙箱开关、在线运行代码开关、管理员口令、运行信息 |

日志保留策略：浏览记录 30 天，注册/登录等关键事件 90 天，之后自动清理
（十几个人一天也就几十条，正常不会触发上限）。

---

## 六、两个安全开关（重要）

**在线运行 / 判题学生代码** —— 服务器模式下**默认关闭**。

原因：这个功能是把学生提交的 Python **在你这台服务器上真跑一遍**，
没有容器隔离。公网开放等于把机器借给任何人执行代码（读文件、发请求、扫内网）。

- 想开：先去「系统设置」把**安全沙箱**打开，再打开这个开关。
  沙箱会拦掉起进程/联网/写文件/动态执行，并给子进程加资源上限（CPU 15 秒、内存 512MB）；
- 不想冒这个险（推荐）：保持关闭。学生要动手写代码，
  把本地完整版分发包发给他们，在自己电脑上跑。

后台会在总览页顶部用红色横幅提醒你"代码执行开着但沙箱没开"这种危险组合。

**管理员账号隔离** —— 学生无法通过注册一个叫 `admin` 的账号混进后台
（后台的登录态是独立的 `is_admin` 标记，跟学生账号体系分开）。

---

## 七、日常维护

**备份**：把整个 `data\` 目录拷走就是完整备份（账号、进度、日志、头像全在里面）。
建议每周一次。

**改端口**：在 `.env` 里加 `PORT=8080`，同时安全组和防火墙规则也要改成 8080。

**看服务有没有活着**：浏览器能打开就是活着。服务是前台进程，
关掉那个命令行窗口就等于停服；开机自启见下。

**开机自启**（可选）：按 `Win+R` 输入 `shell:startup`，把 `一键上线.bat` 的**快捷方式**
放进去即可。

**换电脑/迁移**：整个文件夹拷过去，再把 `data\` 覆盖过去，双击启动脚本。

---

## 八、出问题怎么查

| 现象 | 原因与处理 |
|---|---|
| 浏览器一直转圈到超时 | 99% 是**安全组没放行 5000**。本机 `http://127.0.0.1:5000` 能开、外网不行，就是这个 |
| 学生注册报 500 | `data\` 目录不可写（权限问题）。运行「上线前自检.bat」会指出来 |
| 多人同时用很卡 | 没开服务器模式。确认 `.env` 里有 `PYMASTER_SERVER_MODE=1`，启动日志应显示「服务器模式（多人公网）」 |
| AI 报"连接超时" | 这条通道偶发卡死（实测 6 次里有 1 次卡 47 秒），平台已内置重试；持续失败就换模型或换服务商 |
| 502 / 页面空白 | 看启动窗口里的报错；多半是依赖没装全，重新双击「一键上线.bat」 |
| 想确认装没装对 | 「上线前自检.bat」逐项体检，比肉眼看日志快 |

---

## 九、这个包和你本机的版本差在哪

同一份代码，靠环境变量切换行为，**没有分叉维护两套**：

| 行为 | 你本机（本地模式） | 服务器（PYMASTER_SERVER_MODE=1） |
|---|---|---|
| Web 服务器 | Flask 单进程 | waitress 多线程（16 线程） |
| 在线运行/判题代码 | 开 | 关（沙箱开关同上） |
| 模型配置 | 一份 `.env`（你自己的） | 每人绑自己的 + 平台兜底 |
| 桌面功能（导出 TXT 到桌面、关联本地文件） | 可用 | 屏蔽（服务器上没有桌面） |
| 访问日志 | 不记（没必要） | 记（后台可查） |
| 注册要求 | 不限制 | 不限制（白名单留空时） |

想临时在服务器上体验本地模式的行为，把 `.env` 里的 `PYMASTER_SERVER_MODE` 改成 `0` 再重启
（但不建议在公网上这么干，尤其是别打开在线运行代码）。
