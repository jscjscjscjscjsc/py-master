# 本地与云服务器部署教程

> 适用于 **PyMaster 教学平台** 和 **AI Master 星辰学习系统**。
> 本文分两大部分：
>
> - **上半部分（本地）**：怎么在自己电脑上跑起来 —— 其实只有一个动作：双击启动脚本。
> - **下半部分（云服务器）**：怎么让 30 人以内的班级通过网址访问。

---

## 目录

**本地**
1. [本机启动（Windows）](#一本机启动windows)
2. [首次配置](#二首次配置)
3. [局域网内让同一间教室的人访问](#三局域网内让同一间教室的人访问)

**云服务器**
4. [先想清楚三件事](#四先想清楚三件事)
5. [买服务器](#五买服务器)
6. [上传项目](#六上传项目)
7. [部署与开机自启](#七部署与开机自启)
8. [绑定域名与 HTTPS](#八绑定域名与-https可选但强烈建议)
9. [安全：这是最重要的一节](#九安全这是最重要的一节)
10. [日常维护](#十日常维护)
11. [常见故障排查](#十一常见故障排查)

---
---

# 本地

## 一、本机启动（Windows）

### 1. 解压

把压缩包解压到**任意目录**：

- 路径带中文、带空格都没关系（`D:\课程\PyMaster 教学平台` 可以）；
- **必须"解压到文件夹"**，不要在压缩包窗口里直接双击里面的文件 ——
  那样程序读不到同目录的依赖，必然启动失败。

### 2. 双击启动脚本

解压后根目录只有**一个** `.bat`：

| 平台 | 启动脚本 |
| --- | --- |
| PyMaster | `0-启动PyMaster.bat` |
| AI Master | `0-启动AIMaster.bat` |

双击它，会出现一个黑色命令行窗口：

```
  ============================================================
     PyMaster 教学平台    Python 自学系统
  ============================================================

  第一次启动会自动准备运行环境（约 1 分钟，之后秒开）。
  ...
```

**第一次启动要等 30 秒到 1 分钟**（在解压随包的 Python、离线安装依赖）。
之后每次启动都是秒开。

窗口里出现这样的字样就说明好了：

```
[PyMaster] Started at http://127.0.0.1:5000
```

浏览器会**自动打开**平台首页。没自动打开就手动访问：

- PyMaster：<http://127.0.0.1:5000>
- AI Master：<http://127.0.0.1:5178>

### 3. 停止

**关掉那个黑色窗口就是停止服务。** 窗口关掉，网址就打不开了 —— 这是正常的。

> 💡 想边用边关掉窗口？做不到，别关。可以最小化。

### 不需要装任何东西

分发包里自带：

- 嵌入式 Python 3.12（约 11MB）
- 全部依赖的离线安装包

所以你的电脑上**可以完全没有 Python**，也不需要联网安装依赖。
这也是为什么压缩包有 50–90MB —— 那点体积换的是"学生拿到就能用"。

---

## 二、首次配置

第一次打开时会看到模型配置页。

### 如果暂时没有 API Key

点页面底部的 **「暂时不配置，先进去看看 →」**。

平台会直接放行，**课程、练习、判题、刷题、进度全部可用**，
只有 AI 相关的功能（答疑、AI 批改、教练、图文讲解）会提示"还没配置"。

之后随时点顶部导航的 **「模型配置」** 补上，**补完立刻生效，不用重启**。

### 如果有 API Key

照着 **《大模型添加说明书和教程.md》** 做，五分钟能配完。
最省事的路径：

1. 去 <https://platform.deepseek.com/api_keys> 申请一个 Key；
2. 回到配置页点 **「DeepSeek（推荐）」**（地址和模型名会自动填好）；
3. 把 Key 粘进「API Key」栏；
4. 点「测试连接」→ 看到 ✓ 后点「保存」。

---

## 三、局域网内让同一间教室的人访问

如果只是让学生在自己电脑上各跑各的，**不用看这一节** —— 每台电脑各自解压、各自双击即可。

如果想让**一个老师机当服务器**、学生通过局域网访问，做法是：

### 1. 找到老师机的局域网 IP

在那个黑色命令行窗口里，启动时会打印：

```
 * Running on http://192.168.0.104:5000
```

那个 `192.168.x.x` 就是局域网 IP。

### 2. 学生访问 `http://192.168.0.104:5000`

### 3. 放行防火墙

第一次可能连不上，因为 Windows 防火墙拦了。用管理员身份运行 PowerShell：

```powershell
New-NetFirewallRule -DisplayName "PyMaster" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow
New-NetFirewallRule -DisplayName "AIMaster" -Direction Inbound -LocalPort 5178 -Protocol TCP -Action Allow
```

> ⚠️ **局域网模式没有 HTTPS，也没有真正的隔离。**
> 适合信任环境内的课堂演示；不要当成公网服务用。
> 公网请走下半部分的云服务器方案。

---
---

# 云服务器

## 四、先想清楚三件事

在买服务器之前，请先确认你理解这三件事。它们决定了方案怎么选。

### 1. 这相当于把一台电脑的"运行代码"权限开放出去（**PyMaster 专属风险**）

**先说清楚区别，两个平台的风险不一样：**

| | PyMaster | AI Master |
| --- | --- | --- |
| 会在线执行学生的 Python 代码吗 | **会**（练习场、代码题判题都在服务器上真跑） | **不会**（代码题交给 AI 评分，不在服务器执行） |
| 公网开放的风险 | **高** —— 等于把机器借给别人 | 低 —— 只是个普通网站 |

所以：**如果你只部署 AI Master，这一节的压力小很多**，
但账号体系与模型 Key 那两条仍然要照做。

对 PyMaster：

- **绝对不要**把"在线运行代码"直接对公网开放；
- 要么只给班级/评审这类**可控人群**用（靠账号体系 + 访问控制）；
- 要么打开平台自带的安全闸门（下面第七节第 5 步，**评审/公开场景必开**）；
- 小规模教学最稳的做法是**配学生账号，不公开注册**。

### 2. 必须有账号体系，不能让所有人共用一个

两个平台都支持多账号，学习进度按账号分开存。给班级用时：

- 让学生各自注册（数据存服务器上）；
- 或者由你批量创建账号发给学生；
- **不要**让全教室共用同一个账号 —— 进度会混在一起。

### 3. 模型 Key 只配一份，放在服务器上

服务器上配一把管理员提供的 Key，所有学生共用这把 Key 调用模型。
好处是学生不用自己申请 Key；代价是**额度是共享的**，用超了大家一起停。

- 建议选有免费额度、或按量付费便宜的服务商；
- 在配置页把「备用模型」填上，主模型额度耗尽时会自动切换。

---

## 五、买服务器

### 推荐配置

| 项目 | 推荐 | 说明 |
| --- | --- | --- |
| 地区 | **香港 / 新加坡** | 免备案，当天可用。国内节点要备案，等 1–2 周 |
| 配置 | **2 核 4G** | 2G 内存跑判题容易 OOM；4G 稳妥 |
| 系统 | **Ubuntu 22.04 / 24.04 LTS** | 本文命令都按 Ubuntu 写 |
| 带宽 | 3–5 Mbps 起 | 教材里有图片，带宽太低加载慢 |
| 磁盘 | 40GB+ | 两个平台加起来约 300MB，余量给数据 |

### 常见选择

- 阿里云 / 腾讯云的**香港轻量应用服务器**：约 ¥30–60/月，最省事；
- 其他海外 VPS（Vultr、DigitalOcean 等）：按小时计费，$6–12/月。

> 买完你会得到三样东西：**公网 IP**、**root 密码**（或 SSH 密钥）、
> 以及服务商控制台里的**防火墙/安全组**设置页。

### 先开好端口

在服务商控制台的**防火墙 / 安全组**里，放行这些端口：

- `22`（SSH，必须）
- `80`（HTTP，绑域名用）
- `443`（HTTPS，绑域名用）

> 如果暂时不绑域名，也可以放行 `5000` / `5178` 直接按 IP 访问 ——
> 但这样是**明文 HTTP**，只适合临时演示。

---

## 六、上传项目

### 方法一：用 scp（推荐，最直接）

在你的 Windows 电脑上打开 PowerShell 或 Git Bash，进到项目目录的**上一级**：

```bash
# PyMaster
scp -r "PyMaster 教学平台" root@你的服务器IP:/root/

# AI Master
scp -r "AI Master 学习平台" root@你的服务器IP:/root/
```

> 第一次连接会问 `Are you sure you want to continue connecting?` —— 输 `yes`。
> 然后输 root 密码。

> 💡 **只上传需要的部分**，别把开发目录整个传上去。
> 用打包好的分发包（`dist/` 里的那个 zip）最干净 ——
> 解压后就是完整可运行的目录，还自带 Python。

### 方法二：用分发包

1. 先把分发包 zip 传到服务器：
   ```bash
   scp "PyMaster_教学平台.zip" root@你的服务器IP:/root/
   ```
2. 登进服务器解压：
   ```bash
   ssh root@你的服务器IP
   cd /root
   unzip -q PyMaster_教学平台.zip
   ```

### 登录服务器

```bash
ssh root@你的服务器IP
```

---

## 七、部署与开机自启

### 1. 装 Python 与依赖

服务器上**没有** Windows 版的随包 Python（那是 win 版的），
所以在 Linux 上要用系统自带的 Python：

```bash
apt update
apt install -y python3 python3-venv python3-pip unzip

cd /root/PyMaster\ 教学平台          # 目录名带空格要用反斜杠转义
python3 -m venv runtime/venv
# 装两份：基础依赖 + 教材里学生要用到的库（pandas/requests/pytest 等）
./runtime/venv/bin/pip install -i https://pypi.tuna.tsinghua.edu.cn/simple \
    -r requirements.txt -r requirements-teaching.txt
```

> ⚠️ **别只装 `requirements.txt`** —— 它只有 flask 一项。
> 少装 `edge_tts` 会让语音功能报错；
> 少装 `requirements-teaching.txt` 里那几个，学生做到对应章节时
> 在在线编辑器里跑代码会 `ModuleNotFoundError`（第 13/17/24/26/28 章）。

AI Master 的依赖少得多，只有两项：

```bash
cd /root/AI\ Master\ 学习平台
python3 -m venv runtime/venv
./runtime/venv/bin/pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```

> 想省事也可以直接跑平台自带的环境自举脚本，它会自己建 venv 并装依赖：
> ```bash
> python3 tools/bootstrap_runtime.py
> ```
> 它有一个副作用：会把 Windows 版的 `runtime/python-embed.zip` 也解压一遍
> （在 Linux 上解出来是没有用的 `.exe`，多占十几 MB）。介意的话就照上面手建 venv。

### 2. 配置模型

直接写 `.env`（比在服务器上开浏览器点配置页方便）：

```bash
cd /root/PyMaster\ 教学平台
cat > .env <<'EOF'
PYMASTER_AI_BASE_URL=https://api.deepseek.com/v1
PYMASTER_AI_MODEL=deepseek-chat
ARK_API_KEY=sk-你的密钥
PYMASTER_AI_FALLBACK_MODELS=
EOF
```

AI Master 的变量名不同（**这一点最容易搞错**）：

```bash
cd /root/AI\ Master\ 学习平台
cat > .env <<'EOF'
STARLAB_AI_BASE_URL=https://api.deepseek.com/v1
STARLAB_AI_MODEL=deepseek-chat
ARK_API_KEY=sk-你的密钥
STARLAB_AI_FALLBACK_MODELS=
STARLAB_AI_THINKING=disabled
EOF
```

> ⚠️ 两个平台的地址变量名不一样：
> PyMaster 用 `PYMASTER_AI_BASE_URL`，AI Master 用 `STARLAB_AI_BASE_URL`。
> **把 PyMaster 的 .env 直接复制给 AI Master 会静默失效** ——
> 表现为 endpoint 回落到默认地址、然后报"模型鉴权失败"，很容易误判成 Key 坏了。
> `ARK_API_KEY` 这个键名两边是一样的。

### 3. 先手动跑一次，确认没问题

```bash
cd /root/PyMaster\ 教学平台
PORT=5000 ./runtime/venv/bin/python app.py
```

看到 `Started at http://0.0.0.0:5000` 就说明好了。
按 `Ctrl+C` 停掉，然后配成服务。

### 4. 配成 systemd 服务（开机自启 + 崩溃自动重启）

**这一步很重要** —— 否则一关 SSH 服务就停了。

```bash
cat > /etc/systemd/system/pymaster.service <<'EOF'
[Unit]
Description=PyMaster Teaching Platform
After=network.target

[Service]
Type=simple
WorkingDirectory=/root/PyMaster 教学平台
Environment=PORT=5000
Environment=PYMASTER_OPEN_BROWSER=0
ExecStart=/root/PyMaster 教学平台/runtime/venv/bin/python app.py
Restart=always
RestartSec=5
User=root

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable pymaster
systemctl start pymaster
systemctl status pymaster
```

AI Master 同理（注意端口是 `5178`，目录名不同）：

```bash
cat > /etc/systemd/system/aimaster.service <<'EOF'
[Unit]
Description=AI Master Starlab Platform
After=network.target

[Service]
Type=simple
WorkingDirectory=/root/AI Master 学习平台
Environment=PORT=5178
Environment=STARLAB_OPEN_BROWSER=0
ExecStart=/root/AI Master 学习平台/runtime/venv/bin/python app.py
Restart=always
RestartSec=5
User=root

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable aimaster
systemctl start aimaster
```

> 📌 systemd 的 `WorkingDirectory` 里有空格时**不用加引号**，
> 但 `ExecStart` 的路径要写全 —— 上面已经处理好了，照抄即可。

### 5. 打开安全闸门（PyMaster 在公网场景必做）

**PyMaster 内置**了安全模式，会拦住系统级代码（起进程、开网络、写文件、动态执行），
并给判题子进程加上 CPU 与内存上限。

**评审、公开演示、面向不确定人群 —— 必须打开**：

在 PyMaster 的 service 文件里加上这一行：

```ini
Environment=PYMASTER_SAFE_MODE=1
```

改完 `systemctl daemon-reload && systemctl restart pymaster`。

如果想更保守（完全不允许在线运行代码，课程/刷题/进度等其余功能不受影响）：

```ini
Environment=PYMASTER_ALLOW_CODE_EXEC=0
```

> 安全模式的实现见项目里的 `sandbox_guard.py`。
> **AI Master 没有这个开关** —— 因为它压根不在服务器上执行代码，
> 不需要（上面第四节第 1 点那张表）。
> 如果你在 AI Master 的 service 里写了 `STARLAB_SAFE_MODE=1`，
> 它不会报错，但**也不会有任何作用**，属于写了个寂寞。

---

## 八、绑定域名与 HTTPS（可选，但强烈建议）

### 为什么不直接 `http://IP:5000`？

- 明文传输：学生的账号密码、答题内容在网络上可被截获；
- 浏览器会显示"不安全"警告，学生不敢用；
- 微信扫码登录之类的功能**必须**有公网域名才能用。

### 1. 解析域名

在你的域名服务商那里，加一条 A 记录：

```
pymaster.你的域名.com  →  你的服务器IP
```

### 2. 用 Caddy 自动配 HTTPS（最省事）

Caddy 会自动申请并续期 Let's Encrypt 证书，**不需要你手动折腾证书**：

```bash
apt install -y debian-keyring debian-archive-keyring apt-transport-https curl
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' | gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' | tee /etc/apt/sources.list.d/caddy-stable.list
apt update && apt install -y caddy
```

编辑 `/etc/caddy/Caddyfile`：

```
pymaster.你的域名.com {
    reverse_proxy 127.0.0.1:5000
}

aimaster.你的域名.com {
    reverse_proxy 127.0.0.1:5178
}
```

重载：

```bash
systemctl reload caddy
```

访问 `https://pymaster.你的域名.com` 就自动是 HTTPS 了。

> 项目里的 `deploy/` 目录其实已经带了现成的 Dockerfile、docker-compose
> 和 Caddyfile 配置，还有 `tools/deploy_server.py` 一条命令部署脚本。
> 如果你想用 Docker 路线，直接照 `上线操作手册.md` 走；
> 上面这份是"不用 Docker、纯手工"的版本，依赖更少。

### 3. 微信扫码登录（可选）

如果你想用微信扫码，需要在**微信开放平台 → 网站应用**申请，
审核通过后拿到 AppID / Secret，然后在 `.env` 里填：

```ini
WECHAT_APP_ID=你的AppID
WECHAT_APP_SECRET=你的Secret
OAUTH_CALLBACK_BASE=https://pymaster.你的域名.com
```

注意：**必须是已备案的公网域名**，`127.0.0.1` 打不通。

---

## 九、安全：这是最重要的一节

### 必须做的事

- [ ] **打开安全模式**（上面第七节第 5 步）
- [ ] **关闭公开注册**（如果只给班级用），或给学生发放固定账号
- [ ] 域名走 HTTPS
- [ ] 服务器 root 密码用强密码，最好改成 SSH 密钥登录
- [ ] 定期 `apt update && apt upgrade`

### 一个真实的坑：别把宿主机 data 目录整个挂进容器

如果你走 Docker 路线，**不要把宿主机的 `data/` 整个挂载到容器的数据目录** ——
那样会把镜像里 `data/` 的课程与题库一起盖掉，
表现为**首页直接报错、刷题页空白**，而且报错信息完全指不到原因。

正确做法是设 `PYMASTER_DATA_DIR`（或 `STARLAB_DATA_DIR`）指向数据卷，
容器的 `entrypoint.py` 会在空卷上自动播种数据。

### 别忘了模型 Key 的成本

服务器上的 Key 是**所有学生共用**的。一个学生狂刷 AI 答疑，
可能半天就把额度用光。建议：

- 用有免费额度的服务商起步；
- 在服务商控制台设置**消费上限**；
- 配置页填上备用模型，额度耗尽时自动切换。

---

## 十、日常维护

### 常用命令

```bash
# 看服务状态
systemctl status pymaster
systemctl status aimaster

# 看实时日志（排查问题就看这个）
journalctl -u pymaster -f
journalctl -u aimaster -f

# 重启
systemctl restart pymaster

# 停止
systemctl stop pymaster

# 改完 .env 后让它生效
systemctl restart pymaster
```

### 备份

所有数据都在 `data/` 目录里（账号、学习进度、错题、教练会话）：

```bash
cd /root/PyMaster\ 教学平台
tar czf /root/backup-$(date +%F).tar.gz data/
```

建议加一个每日定时备份：

```bash
# crontab -e，加一行（每天凌晨 3 点备份）
0 3 * * * cd /root/PyMaster\ 教学平台 && tar czf /root/backup-$(date +\%F).tar.gz data/
```

> 恢复：停服务 → 解压覆盖 `data/` → 起服务。

### 更新版本

在本机重新打包 → 上传 → 服务器上覆盖代码（**保留 `data/` 和 `.env`**）：

```bash
cd /root
mv PyMaster\ 教学平台 PyMaster_old
unzip -q PyMaster_教学平台.zip
mv PyMaster\ 教学平台 PyMaster\ 教学平台_new || true
# 把老的数据和配置搬回来
cp -r PyMaster_old/data PyMaster\ 教学平台/
cp PyMaster_old/.env PyMaster\ 教学平台/
systemctl restart pymaster
```

---

## 十一、常见故障排查

### 服务起不来

```bash
journalctl -u pymaster -n 50 --no-pager
```

对着报错看：

| 报错 | 原因 | 处理 |
| --- | --- | --- |
| `ModuleNotFoundError: flask` | 依赖没装 | 回到第七节第 1 步 |
| `Address already in use` | 端口被占 | `ss -lntp \| grep 5000` 找到进程杀掉 |
| `Permission denied` | 文件权限 | `chmod +x` 或用 root 跑 |
| 目录不存在 | 路径里的空格没转义 | `ExecStart` 路径要写全，参考上面现成的 service 文件 |

### 浏览器打不开

1. 服务在跑吗？`systemctl status pymaster`
2. 端口通吗？`curl -I http://127.0.0.1:5000`（在服务器上执行）
   - 服务器上通、外面不通 → **服务商安全组/防火墙没放行**
3. 域名解析对吗？`ping 你的域名` 看看是不是你的 IP
4. Caddy 起来了吗？`systemctl status caddy`

### AI 功能报"调用失败"

1. 先确认 `.env` 里的**变量名对**（PyMaster 用 `PYMASTER_AI_*`，AI Master 用 `STARLAB_AI_*`）；
2. 确认服务器能访问服务商：
   ```bash
   curl -I https://api.deepseek.com
   ```
   如果超时 → 服务器的网络出不去，或服务商封了海外 IP（换个服务商试试）；
3. 打开配置页点「测试连接」，它会直接告诉你错在哪；
4. Key 有额度吗？去服务商控制台看余额。

### 学生做题时"判题很慢"或超时

- 2G 内存的机器判题容易 OOM —— 升级到 4G；
- 安全模式会给判题加上 15 秒 CPU 上限，这是**故意的**（防止死循环拖死服务器）；
  正常题目不会触到这个上限。

### 想让平台只能内网访问

在 Caddyfile 里加上 IP 白名单：

```
pymaster.你的域名.com {
    @internal remote_ip 192.168.1.0/24
    handle @internal {
        reverse_proxy 127.0.0.1:5000
    }
    respond 403
}
```

---

## 附：最省事的部署路径（懒人版）

如果你不想折腾，就这样：

1. **买**：阿里云/腾讯云香港轻量 2核4G Ubuntu 22.04；
2. **传**：`scp -r "PyMaster 教学平台" root@IP:/root/`；
3. **装**：
   ```bash
   ssh root@IP
   apt update && apt install -y python3-venv unzip
   cd /root/PyMaster\ 教学平台
   python3 -m venv runtime/venv
   ./runtime/venv/bin/pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
   echo 'PYMASTER_AI_BASE_URL=https://api.deepseek.com/v1
   PYMASTER_AI_MODEL=deepseek-chat
   ARK_API_KEY=sk-你的密钥' > .env
   PORT=5000 nohup ./runtime/venv/bin/python app.py > /root/pm.log 2>&1 &
   ```
4. **访问**：`http://服务器IP:5000`（记得先在安全组放行 5000）

> 这个版本**没配开机自启**，服务器一重启服务就没了 ——
> 只是让你先看到效果。确认没问题后再回到第七节配 systemd。
