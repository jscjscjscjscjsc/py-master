# PyMaster 演进记录

> 目标：把 PyMaster 从「Python 基础语法演示 Demo」演进为「从语法到 AI 应用开发的实用学习平台」，
> 并把每个知识点下方的语音讲解换成 **5 分钟高密度 AI 图文讲解**。

本文档按阶段记录已经落地的改动、可复现的操作命令、以及下一步编排。

---

## 一、当前状态总览

| 维度 | 演进前 | 现在 |
|---|---|---|
| 章节数 | 9 章（Python 语法） | **39 章**（第 1–35 章语法/工程/全栈/AI + 第 37–40 章 AI 进阶） |
| 知识点数 | 40 个 | **400 个** |
| 练习题库 | 内置少量选择题 | **212 道教材原题，全部带参考答案与解析** |
| 每章思维导图 | 手工维护 | 由小节标题树自动生成 |
| 知识点讲解 | 角色对白漫画（纯文字气泡，1–2 分钟） | **5 分钟图文讲解**：漫画插画 + 讲义 + 字幕 + 口播 |
| 讲解生产方式 | 人工预写 JSON | **AI 流水线**（大模型写分镜 → Seedream 生图 → edge-tts 口播） |
| 生成能力 | 无 | 平台内可**现场生成**任一知识点的讲解（后台任务 + 进度轮询） |

---

## 二、阶段一：教材内容迁移（已完成）

### 做了什么

把 `桌面/数据文件/教材正文/`（Day01–Day14，35 章）与
`桌面/数据文件/AI全栈开发40天课程体系/教材正文/`（4 章）的 Markdown 教材
转换为平台课程数据 `data/courses.json`。

转换规则：

- `## N.X 标题` → 一个知识点
- 「本章练习题」→ 练习题库（并与 `第N章_练习题答案.md` 合并出参考答案与解析）
- Debug 四件套 / Vibe Coding 任务卡 / 小结与知识树 / 本章速查 → 打上类型标记，前端可做差异化样式
- 小节标题树 → 自动生成每章思维导图

### 产物与命令

```bash
python tools/migrate_courseware.py --check    # 只体检，不写文件
python tools/migrate_courseware.py --write     # 写入 data/courses.json
```

- `tools/md2html.py` —— 教材 Markdown → 富文本 HTML（代码块 / 表格 / 引用 / 嵌套列表 / ✅❌⚠ 教学标记）
- `data/courses_legacy_9ch.json` —— 迁移前的旧课程数据备份

### 章节分篇

| 篇 | 章号 | 内容 |
|---|---|---|
| 基础篇 · Python 核心语法 | 1–14 | 环境、变量、流程、容器、函数、OOP、文件 IO、综合训练 |
| 工程篇 · 软件工程与协作 | 15–20 | 需求、设计、测试、Git 分支、Gitee、Code Review |
| 全栈篇 · 后端 / 前端 / 数据 | 21–26 | 架构、HTML/CSS/JS、HTTP/REST/SQLite、FastAPI、Vue、pandas |
| AI 篇 · 大模型应用开发 | 27–35 | 大模型 API、DeepSeek 实战、智能体框架、五个模块的综合实战 |
| AI 进阶篇 · RAG 与 Agent 工程 | 37–40 | LLM API 工程化、RAG 检索增强、Agent 核心机制、Agent 框架实战 |

---

## 三、阶段二：5 分钟图文讲解引擎（已完成，并已切换到程序化作图）

### 三级流水线

```
知识点 HTML
   ↓ ① 分镜脚本（火山方舟大模型）
   ↓    16 个镜头：口播稿 / 字幕要点 / 结构化作图规格 visual / 可选代码
   ↓    字数不足时自动定向补写（见下方"补写工序"）
   ↓ ② 配图（程序化作图，不依赖任何生图接口）
   ↓    16 张 1080×1080 课件图，7 种版式
   ↓ ③ 口播（edge-tts 免费）逐镜头切分音频，便于图文同步
5 分钟讲解（可播放 / 可跳转 / 可逐句高亮）
```

### 配图为什么改成程序化作图

早期版本调用第三方生图模型（Seedream），存在三个问题：

1. **画不对字**。技术课件里全是中文关键词、代码、对照表，
   生成式模型会画出乱码伪文字（实测屏幕上出现 `O ll 1IOUCI.US` 这类东西）。
2. **风格不稳定**。同一节讲解里会混进像素风、写实风，画风随提示词漂移。
3. **成本与额度**。批量生产 400 节需要 6400 张图，第三方接口难以大量使用。

改为程序化作图后，版面由代码精确控制：中文、代码、对照表都正确，风格完全统一，
渲染速度 8–14 张/秒（一节的 16 张图 1–2 秒出完），且可以无限量生产。

### 程序化作图的实现

| 文件 | 作用 |
|---|---|
| `tools/render/visuals.mjs` | 设计系统：调色板、吉祥物 SVG、7 种版式的排版函数、Python 语法着色 |
| `tools/render/build-plan.mjs` | 把分镜里的 `visual` 规格编译成每张图的版面 HTML |
| `tools/render/render_plan.py` | 用 Python Playwright 的 Chromium 批量截图（复用同一浏览器会话） |

7 种版式对应镜头类型：

| layout | 用途 | 版式 |
|---|---|---|
| `hook` | 开场 | 对话气泡 + 吉祥物 + 一句钩子 |
| `concept` | 概念 | 3–4 条要点卡 + 吉祥物 + 结论条 |
| `code` | 代码 | 语法着色代码卡 + 运行结果框（长行自动缩放字号） |
| `compare` | 对比 | 左右两栏 + VS + 结论 |
| `pitfall` | 踩坑 | 错误代码 / 正确代码上下对照 |
| `summary` | 小结 | 3 张编号结论卡 |
| `flow` | 流程 | 编号步骤 + 箭头 |

关键实现细节：

- **吉祥物用纯 SVG 画死**（一条戴圆框眼镜的绿蛇 + 一只黄小鸡），
  每张图形象完全一致，不会像生成式模型那样每张都换长相。
- **文案硬裁剪**。出图是精确排版，超一个字就会换行撑破卡片，
  所以 `sanitize_visual()` 会逐字段按字数上限裁剪，宁可文案略短也不让版面崩掉。
- **代码不截断**。早期按 34 列硬截断会把代码截成语法错误的半行，
  现在放宽到 60 列，宽度交给渲染端按最长行自动缩放字号。

### 关键实现

| 文件 | 作用 |
|---|---|
| `narration_engine.py` | 引擎：`ArkText`（分镜）/ `VoiceOver`（口播）/ `NarrationBuilder`（组装）/ `sanitize_visual`（规格裁剪） |
| `build_narrations.py` | 批量生产 CLI，支持按章 / 按篇 / 限量 / 强制重做 |
| `tools/batch_stage.sh` | 分批跑批脚本，失败自动重试补缺口 |
| `static/js/narration_player.js` | 播放器：镜头切换、口播同步、逐句高亮、自动连播、倍速、镜头跳转 |
| `static/css/narration.css` | 播放器样式：暗色舞台 + 宣纸质感讲义区 |
| `data/narrations.json` | 分镜脚本索引（key: `<章号>_<知识点序号>`） |
| `static/narrations/<ch>_<kp>/s01.jpg…` | 镜头配图 |
| `data/audio_cache/narr_<hash>.mp3` | 镜头口播（内容寻址缓存） |

### 两个工程细节（踩过坑后固化下来）

1. **补写工序**：大模型对「每个镜头写满 N 字」这类长度指令完成度不稳定，实测只写到七成。
   因此在生成后按实际字数做一次定向补写（`expand_short_scenes`），
   把讲解从 4:12 稳定拉到 **5:06**。
2. **画风锁定**：风格提示词里显式排除像素风 / 8-bit，并要求界面元素只用色块示意、
   不画任何文字；否则同一次生成里会混进像素风和乱码伪文字，破坏整节讲解的视觉连贯。
3. **播放器不依赖 CDN**：章节页底部有一批 CDN 资源（CodeMirror、three.js），
   课堂网络不稳时会把页面解析卡住。图文讲解是核心学习入口，
   因此 `narration_player.js` 放在 `<head>` 加载，
   实测外部资源仍在 loading 时播放器已经可用。

### 实测数据（第 5 章 · 5.2 元组：不可变序列）

```
16 个镜头 / 口播 1702 字 / 总时长 306 秒（5:06）/ 构建耗时 281 秒
配图 5.1 MB + 音频 1.8 MB = 6.8 MB
```

### 产物与命令

```bash
python build_narrations.py --list                          # 看制作进度
python build_narrations.py --chapter 5 --kp 2               # 做一章一节
python build_narrations.py --chapter 5                      # 做整章
python build_narrations.py --stage 基础篇 --limit 8         # 按篇批量
python build_narrations.py --chapter 5 --kp 2 --no-images   # 只出脚本与口播（省钱）
```

### 平台内接口

| 接口 | 说明 |
|---|---|
| `GET /api/narration/<ch>/<kp>` | 取分镜（含每镜头配图、时长、音频地址） |
| `GET /api/narration/audio/<ch>/<kp>/<scene>` | 镜头口播 mp3 |
| `GET /api/narration/stats` | 全站讲解覆盖率（按章统计） |
| `GET /api/narration/status` | 文本模型 / 生图模型 / 语音 是否就绪 |
| `POST /api/narration/request` | **现场生成**某知识点的讲解（后台线程） |
| `GET /api/narration/job/<job_id>` | 生成进度轮询 |

学生端入口：章节页每个知识点下方的 **🎬 5 分钟图文讲解** 按钮。
讲解尚未生成时，界面会提示并提供「现在为这一节生成讲解」，带实时进度输出。

### 配置

`.env` 中与讲解相关的项：

```
ARK_API_KEY=...                              # 分镜脚本 + 生图共用
PYMASTER_AI_MODEL=doubao-seed-2-0-code-preview-260215
PYMASTER_IMAGE_MODEL=doubao-seedream-4-0-250828
```

口播使用 edge-tts，**免费、无需密钥**。

---

## 四、阶段三：LangFlow 教学编排（调研完成，待落地）

已完成对 LangFlow 开源项目的深度调研（官方仓库 / 文档 / PyPI / 发布记录），
并产出 8 章、每章 4–6 个知识点的教学大纲，含每节的教学内容、实操任务、
常见坑与前置 Python 知识对应关系。主线：

```
装好环境搭第一个可视化 LLM Flow
  → 让 Flow 变成接口（变量 / 密钥 / /run API）
  → RAG Flow（让 Flow 回答私有资料）
  → Agent + Tools Flow（会动手的 Flow）
  → MCP 双向接入（Flow 既是工具，也用工具）
  → 工程化（自定义组件 + FastAPI 封装）
  → 部署与运维（Docker / 数据库 / 安全基线）
  → 结课项目：企业知识库问答机器人
```

关键事实（教学必须锁版本）：MIT 协议、约 15.5 万 star、当前稳定版 v1.12.1、
要求 Python 3.10–3.14、默认端口 7860（被占用时会自动换端口）、
默认 SQLite 生产建议 PostgreSQL、1.11 起 Docker 镜像默认关闭自动登录。

> 落地方式：新增「AI 智能体编排篇」章节（接在 AI 进阶篇之后），
> 补充市场调研、竞品矩阵与教学痛点分析，再按同一套教材格式写入课件并跑迁移工具。

---

## 五、下一步编排

### A. 批量生产进度

**当前状态：等模型账号充值后继续。** 火山方舟账号欠费，
接口返回 `HTTP 403 AccountOverdueError: your account has an overdue balance`，
三个模型（主模型与两个备用）全部不可用，讲解生成因此停摆。

```bash
bash tools/batch_stage.sh            # 全部章节，直到做完（默认并发 4）
bash tools/batch_stage.sh "基础篇"    # 只做某一篇
python build_narrations.py --list     # 看进度（不会打断生产）
```

脚本开跑前会先探一次模型接口，欠费/密钥失效会**直接退出并说明原因**，
不会空转几十轮。

| 篇 | 进度 |
|---|---|
| 基础篇 · Python 核心语法 | 61 / 148 |
| 工程篇 · 软件工程与协作 | 0 / 52 |
| 全栈篇 · 后端 / 前端 / 数据 | 0 / 58 |
| AI 篇 · 大模型应用开发 | 0 / 99 |
| AI 进阶篇 · RAG 与 Agent 工程 | 0 / 43 |
| **合计** | **61 / 400（15.2%）** |

### B. 恢复额度后的预计耗时

生产已支持并发（`--kp-workers`，默认 4 节同时做）。单节的耗时主要在大模型写稿
（约 130 秒），口播与配图加起来不到 5 秒，所以并发能近乎线性提速：

|  | 串行 | 并发 4 |
|---|---|---|
| 剩余 339 节预计 | 约 12 小时 | **约 3 小时** |

> 并发能力用本地假模型（`tools/mock_llm.py`）实测验证过：3 节并发全部成功、
> 16 张配图齐全、口播无缺口。同时修掉了一个真实的并发 bug——
> 多节同时合成口播时，相同文本会命中同一缓存文件并撞出 WinError 32，
> 现已改为「同一文本加锁串行 + 先写临时文件再原子替换」。

### C. 后续优先级

按章号顺序推进即可（1 → 40），这样基础篇会先补完，随后自然进入 AI 篇。
如果想先要课程卖点部分，也可以直接 `--stage "AI 篇"`。

### D. 待办清单

- [ ] 等账号充值后跑完全部 400 节讲解
- [ ] 章节开场 3D 动画目前只有 9 套分镜（已按主题重映射到新章号），第 8、10 章及以后暂无开场
- [ ] 旧版 `data/comics.json`（40 组角色对白，按旧 9 章编号）在新章节体系下已无对应
- [ ] 微信扫码登录需要微信开放平台资质与公网回调域名（代码已就绪，见第七节）
- [ ] LangFlow 开源项目的教学编排（调研已完成，暂缓落地）

### E. LangFlow 教学编排（调研完成，待落地）

已完成对 LangFlow 开源项目的深度调研（官方仓库 / 文档 / PyPI / 发布记录），
并产出 8 章、每章 4–6 个知识点的教学大纲，含每节的教学内容、实操任务、
常见坑与前置 Python 知识对应关系。主线：

```
装好环境搭第一个可视化 LLM Flow
  → 让 Flow 变成接口（变量 / 密钥 / /run API）
  → RAG Flow（让 Flow 回答私有资料）
  → Agent + Tools Flow（会动手的 Flow）
  → MCP 双向接入（Flow 既是工具，也用工具）
  → 工程化（自定义组件 + FastAPI 封装）
  → 部署与运维（Docker / 数据库 / 安全基线）
  → 结课项目：企业知识库问答机器人
```

关键事实（教学必须锁版本）：MIT 协议、约 15.5 万 star、当前稳定版 v1.12.1、
要求 Python 3.10–3.14、默认端口 7860（被占用时会自动换端口）、
默认 SQLite 生产建议 PostgreSQL、1.11 起 Docker 镜像默认关闭自动登录。

---

## 六、练习题系统修复与逐题实测

### 修了什么

教材的 212 道练习题迁移进来后**全部渲染成空白框**，代码编辑器也打不开。根因有三个：

1. **题型没渲染**。模板只处理 `choice` / `code` 两种题型，
   而教材原题统一被标成 `open`，于是每道题只剩一个空壳。
   现在新增 `open` 题型分支：题干按富文本渲染，并提供
   「写代码作答」「文字作答」「查看参考答案」「请教 AI 助教」四个入口。

2. **CDN 拉不下来导致编辑器失效**。CodeMirror、d3、markmap 都从
   `cdn.jsdelivr.net` 加载，网络不稳时这些脚本全部失败，
   `CodeMirror.fromTextArea` 报错，编辑器区域一片空白。
   现已全部本地化到 `static/vendor/`（共 1.5MB），教室里断网也能用。

3. **正文样式缺失**。迁移生成的 `.md-codeblock` / `.md-table` /
   `.md-callout` 等类名没有任何 CSS，400 个知识点的正文都是浏览器默认样式。
   新增 `static/css/md_content.css` 统一排版。

另外修掉两个小问题：`showExAnswer` 在非选择题上会因取不到选项而抛错
（表现为"查看参考答案"点了没反应）；绘图题会因为 matplotlib 默认去初始化
GUI 后端而**卡到超时**，现在给代码运行器强制 `MPLBACKEND=Agg`，
第 26 章的绘图题从 10 秒超时变成 0.7 秒跑通。

### 逐题实测

新增 `tools/test_exercises.py`，对全部题目做三道检查：

```bash
python tools/test_exercises.py            # 渲染 + 运行（全量，不花钱）
python tools/test_exercises.py --ai 20    # 额外抽 20 道做 AI 评分
```

最新结果：

| 检查 | 结果 |
|---|---|
| ① 渲染：题干与作答入口 | **398 / 398 全部通过** |
| ② 代码运行：参考答案可独立运行 | 75 段 |
| ② 需交互输入（跳过） | 35 段 |
| ② 预期失败（依赖学生自建模块 / 需先建数据 / 需配置密钥 / 故意抛异常演示） | 27 段 |
| ② 教学片段不可独立运行（答案片段依赖前文，非完整程序） | 12 段 |
| ② **真失败** | **0 段** |
| ③ AI 评分（抽样） | 代码题 10/10 成功均分 99；文字题 10/10 均分 91 |

> 关于"参考答案跑不通"：教材答案里有相当一部分是**教学节选**，天生不能独立运行
> （例如只给 `@app.get(...)` 函数而 `app = FastAPI()` 在上一段；只给 `import calc`
> 而 calc.py 是学生上一题自己写的；题目本身要求"找出坏味道"所以示例代码故意不规范）。
> 测试脚本按错误类型与题干关键词做分类，只把"应该能跑却跑不通"的算问题，
> 否则结论没有意义。目前这一类为 0。

### 教学依赖

教材要求学生装库的章节，环境里也装齐了（并写成 `requirements-teaching.txt`）：
`requests` `pytest` `fastapi` `uvicorn` `httpx` `pandas` `matplotlib` `openai`。
国内环境用镜像安装：

```bash
pip install -r requirements-teaching.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 七、首次运行引导：账号注册 + 模型配置

### 为什么做这个

用户下载压缩包、双击启动后，原先要面对的是**黑色终端里的问答**
（`setup_api.py` 逐条 input 提示）。而且还有一个更硬的门槛：
`is_whitelisted()` 在白名单为空时返回 `False`，而 `whitelist.json` 默认就是空的，
导致**注册和登录全被拦住**——新用户根本进不去。

### 现在的首次运行流程

```
双击 启动 PyMaster.bat
   → 浏览器自动打开 http://127.0.0.1:5000
   → 没配好模型时，任何页面都自动跳到 /setup 配置向导
   → 第一步：QQ 邮箱注册（或登录已有账号）
   → 第二步：填接口地址 / 模型名称 / API Key →「测试连接」
   → 第三步：进入平台开始学习
```

**页面不引用任何外部资源**（0 个 CDN 脚本）：首次运行时用户可能还没配好网络，
向导必须能离线打开。这跟之前把 CodeMirror 本地化是同一个考虑。

### 关键实现

| 位置 | 说明 |
|---|---|
| `templates/setup.html` | 三步向导页，自带样式与逻辑，零外部依赖 |
| `app.py · ai_config_status()` | 判断模型配置是否完整，返回脱敏回显 |
| `app.py · apply_ai_config()` | 写 `.env` 并**热更新** `ark` / `AI_MODEL` / `AI_API_KEY`，保存后立即生效不用重启 |
| `app.py · test_ai_connection()` | 真发一次最小请求，把 401/403/404/429 翻译成中文提示 |
| `app.py · require_ai_config()` | 首次运行的 before_request 守卫，把用户引到向导 |
| `app.py · registration_is_open()` | 白名单为空 = 不限制注册（填了名单才生效） |

几个刻意的设计：

- **测试连接的报错要说人话**。401 显示「API Key 不正确或已失效」、
  404 显示「模型名称或接口地址不对」、连不上显示「请检查 Base URL 和网络」，
  而不是把原始 HTTP 错误糊在用户脸上。
- **注册即登录**，不用再手动登一次。
- **支持邮箱登录**：账号填邮箱或填自定义用户名都能登。
- **保存配置不会写坏 .env**：先读原文件、剔除旧的模型配置项、再整段重写，
  其余键（比如 TTS 配置）原样保留。实测保存前后配置一致。

### 模型服务商预设

向导里内置了常用服务商的一键填充：火山方舟 / DeepSeek / 硅基流动 /
通义千问 / 本地 Ollama。点一下自动填好接口地址和示例模型名。

### 微信扫码登录：代码已实现，但需要你去申请

**这一项没法开箱即用**，原因不是技术实现，而是资质：

微信扫码的整个流程要由微信服务器回调到你的地址，回调地址必须是
**已备案的公网域名**。本程序跑在用户自己的电脑上（127.0.0.1），
微信服务器无法回调进来，所以单机版默认关闭并置灰。

OAuth 流程已经完整实现（`/oauth/wechat/start` 与 `/oauth/wechat/callback`，
含 state 防重放、code 换 token、拉取用户信息、按 openid 建本地账号）。
在 `.env` 里补上三项并重启，界面会自动放行：

```
WECHAT_APP_ID=          # 微信开放平台「网站应用」的 AppID
WECHAT_APP_SECRET=      # 对应 AppSecret
OAUTH_CALLBACK_BASE=    # 公网可访问地址，例如 https://your.domain
```

前置条件：去 [微信开放平台](https://open.weixin.qq.com) 注册并创建「网站应用」，
提交审核（需要企业资质与已备案域名）。审核通过拿到 AppID/Secret 后，
把程序部署到有公网域名的服务器上即可。

**在拿到这些之前，两个可用的替代方案**：

1. 同局域网扫码打开：手机连同一个 WiFi，扫 `http://电脑IP:5000` 的二维码即可上课，
   不需要任何资质（适合课堂场景）。
2. 邮箱注册：向导第一步已经能用，零门槛。

---

## 八、已知取舍

- **编程练习的追加规则**：平台会给「正文里含代码块」的知识点自动追加一道编程练习；
  纯概念、流程、协作类知识点不再硬塞代码题（此前是每个知识点都塞）。
  旧的 `code_hints` 关键词表按老章节标题编写，新章节大部分会回退到通用提示
  「用代码把「X」的核心用法演示一遍」，可后续按新知识点补关键词。
- **第 36 号章号空缺**：AI 进阶篇从 37 起编（承接教材原本的第 02–05 章），
  不影响解锁顺序与展示，仅编号不连续。
- **CDN 依赖**：章节页的代码编辑器与 3D 组件仍走公网 CDN。
  图文讲解已改为本地加载不受影响；若要在完全离线的机房上课，
  建议把 CodeMirror 与 three.js 也下到 `static/vendor/`。
