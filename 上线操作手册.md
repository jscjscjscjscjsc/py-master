# PyMaster 上线操作手册

> 目标读者：要让学生真正用上这套平台的人。
> 场景边界先说清楚：**30 人以内、要注册登录、要全功能（含判题与 AI）。**
>
> 最后更新：2026-09-18

---

## 一、结论：最推荐的方案

**一台腾讯云 / 阿里云的「轻量应用服务器」，选香港或新加坡节点，2 核 4G，
配一个自己的域名，用仓库里现成的 Docker 配置跑起来。**

| | 推荐配置 | 说明 |
|---|---|---|
| 服务器 | 轻量应用服务器 · 香港/新加坡 · 2核4G · 60GB SSD | **免备案**，当天就能用 |
| 带宽 | 5 Mbps 起（轻量默认 30 Mbps 峰值） | 30 人看图文讲解够用 |
| 系统 | Ubuntu 22.04 / Debian 12 | 别的也行，装了 Docker 就一样 |
| 域名 | 任意 `.top` / `.xyz` / `.com` | 用于 HTTPS 证书，¥30–80/年 |
| 成本 | 服务器 ¥40–70/月 + 域名 ¥60/年 | 一年约 ¥600，30 人摊每人每月约 ¥1.7 |

一条命令部署（下面第四节有完整步骤）：

```bash
python tools/deploy_server.py --host root@你的服务器IP --domain 你的域名
```

---

## 二、为什么是这个，而不是别的

| 方案 | 成本 | 能不能当天用 | 手机/电脑 | 判题 | 结论 |
|---|---|---|---|---|---|
| **香港/新加坡轻量 + Docker** | ¥600/年 | ✅ 当天 | ✅ | ✅ 全功能 | **推荐** |
| 国内轻量（腾讯云/阿里云） | ¥300–700/年 | ❌ 要备案 7–20 天 | ✅ | ✅ | 已备案过域名就选它，国内最快 |
| 国外 VPS（RackNerd/Vultr 等） | ¥150–400/年 | ✅ 当天 | ⚠️ 150–250ms | ✅ | 更便宜，但国内访问明显慢，讲解视频会卡 |
| 云托管 / PaaS（CloudBase 等） | 按量 ¥10–40/月 | ✅ | ✅ | ❌ 无沙箱判题必须关掉 | 运维最省心，但要牺牲"动手写代码" |
| 家里电脑 + Cloudflare Tunnel | ¥0（要域名） | ✅ | ✅ | ⚠️ 跑在你自己的电脑上 | 适合课堂演示，长期在线不合适 |

三个关键判断：

1. **为什么要 4G 内存不是 2G。** 判题是把学生代码 fork 成子进程真跑一遍，
   第 26 章的数据分析题一 import pandas + matplotlib 就是 200MB 上下。
   2G 内存 5 个人同时做数据分析题就会 OOM。4G 才有余量。
2. **为什么选香港/新加坡，不选国内也不选欧美。** 国内节点最快但要备案，
   学生等着用等不起；欧美 VPS 便宜，但国内访问 150–250ms，图文讲解里
   一节课上百张图，会明显卡。香港/新加坡免备案，国内 30–80ms，是最平衡的点。
3. **为什么不用云托管。** 云托管的容器一挂上公网地址，判题就等于把机器
   借给任何人执行代码（详见第五节）。要么关掉判题，要么放弃云托管。
   30 人这个规模，一台服务器反而更简单也更完整。

---

## 三、买之前必须知道的一件事

**判题和"运行代码"是把学生提交的 Python 在服务器上真跑一遍，没有沙箱。**

这不是"概率问题"，是"被扫到就必然发生"。30 人的小班用，风险可控，
但必须做到两条：

1. **一定套访问口令**（仓库里的 Caddy 配置已经内置，默认就是开的）。
2. **不要把链接发到公开渠道**。口令泄露了跟没设一样。

真要完全安全，我们的做法是：**服务器上把在线运行代码关掉**
（`PYMASTER_ALLOW_CODE_EXEC=0`），课程正文、练习解析、AI 答疑照常，
只有"自己敲代码跑一下"要改用本地版。给低年级用，这个折中方案往往更合适。

---

## 四、30 分钟上线（复制粘贴即可）

### 第 0 步：买服务器和域名（10 分钟）

1. 腾讯云 / 阿里云控制台 → 轻量应用服务器 → 地域选 **中国香港** 或 **新加坡**
   → 镜像选 **Ubuntu 22.04** → 套餐 2核4G → 付款
2. **防火墙 / 安全组放行 80 和 443**（这一步漏了，后面 HTTPS 一定失败）
3. 买一个域名，在 DNS 里加一条 A 记录指向服务器公网 IP

### 第 1 步：把平台推上去（一条命令，5–15 分钟）

在你自己的电脑上（项目根目录）：

```bash
# 先空跑一次，看看会打包什么、多大，不动服务器
python tools/deploy_server.py --host root@你的服务器IP --dry-run

# 正式部署（约 1.4GB，含讲解配图与口播，取决于你的上行带宽）
python tools/deploy_server.py --host root@你的服务器IP --domain 你的域名
```

脚本会：打包 → scp 上传 → 在服务器上装 Docker（如果没装）→
`docker compose up -d --build` → 打印还差哪几步。

> Windows 用 Git Bash 或 PowerShell 都自带 ssh/scp，不用额外装东西。
> 前提是本机能免密登录服务器（`ssh-copy-id` 或把公钥贴到服务器的
> `~/.ssh/authorized_keys`）。用密码登录也行，脚本会提示你输密码。

**想先快速验证服务器能跑通**，加 `--skip-media`（只传 94MB 的代码与数据），
讲解页会是空的，确认没问题后再 `--media-only` 补传讲解产物。

### 第 2 步：设访问口令（3 分钟）

```bash
ssh root@你的服务器IP
cd /opt/pymaster/deploy
cp .env.example .env

# 生成口令哈希（把 '你想设的口令' 换掉）
docker run --rm caddy:2 caddy hash-password --plaintext '你想设的口令'

vi .env      # 填三行：DOMAIN / BASIC_USER / BASIC_HASH

cd /opt/pymaster
docker compose -f deploy/docker-compose.yml up -d caddy
```

打开 `https://你的域名`，浏览器地址栏出现锁头 = HTTPS 证书签好了。

### 第 3 步：填模型密钥（2 分钟）

不填也能用：课程正文、练习、刷题、判题全部离线可跑，
只有 **AI 答疑** 和 **AI 批改** 需要模型。

```bash
ssh root@你的服务器IP
vi /opt/pymaster/deploy/env      # 直接写，没有就新建
```

内容三行：

```
PYMASTER_AI_BASE_URL=https://opencode.ai/zen/go/v1
PYMASTER_AI_MODEL=deepseek-v4.1-flash
ARK_API_KEY=你的密钥
```

存盘后重启：

```bash
cd /opt/pymaster
docker compose -f deploy/docker-compose.yml restart pymaster
```

### 第 4 步：验证（2 分钟）

- 打开 `https://你的域名` → 输口令 → 出现**开场 CG** → 点「跳过片头」
- 注册一个账号 → 进仪表盘 → 点进第 1 章 → 点「🎬 5 分钟图文讲解」，
  应该**有图有声**
- 去刷题中心做一道题，点「▶ 运行」→ 能出结果 = 判题正常工作
- 手机浏览器打开同一个地址 → 能正常浏览（手机上暂不做刷题，界面偏窄）

---

## 五、必做的日常维护

```bash
# 看日志（出问题先看这里）
cd /opt/pymaster && docker compose -f deploy/docker-compose.yml logs -f pymaster

# 备份：账号、进度、笔记、错题全在 runtime-data 里
tar czf ~/pymaster-$(date +%F).tar.gz \
  /opt/pymaster/deploy/runtime-data /opt/pymaster/deploy/env

# 更新代码后重新部署
python tools/deploy_server.py --host root@你的服务器IP
```

**建议每周备份一次**，把 tar 包拉到本地。学生一学期的进度丢了，比服务器挂了严重。

---

## 六、踩过的坑（出问题先来这里对）

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 首页报错 / 刷题页空白 | 把整个 `data/` 挂到容器里，把镜像里的课程与题库盖掉了 | 用 `PYMASTER_DATA_DIR`（仓库里已改好），别改回去 |
| 注册账号报 500 | 挂载卷属主是 root，服务用户写不进去 | `deploy/entrypoint.py` 会自动修属主；若仍失败，`chown -R 10001:10001 /opt/pymaster/deploy/runtime-data` |
| HTTPS 一直签不下来 | 安全组没放 80/443，或 DNS 还没生效 | 放行端口；`dig 你的域名` 确认解析到服务器 IP |
| `docker build` 卡在拉基础镜像 | 国内服务器拉 Docker Hub 很慢或被墙 | 配镜像加速器，见下方命令 |
| AI 答疑一直转圈不出字 | 反向代理缓冲了 SSE | 仓库的 Caddyfile 里已有 `flush_interval -1`，别删 |
| 30 人同时用后变慢 / 502 | 内存不够，判题子进程把机器吃满 | 升到 4G；或关掉在线运行代码 |
| 讲解页说"还没生成" | 讲解产物没上传 | `python tools/deploy_server.py --host ... --media-only` |

国内服务器配 Docker 镜像加速（一次性）：

```bash
mkdir -p /etc/docker
cat > /etc/docker/daemon.json <<'EOF'
{
  "registry-mirrors": [
    "https://mirror.ccs.tencentyun.com",
    "https://docker.m.daocloud.io"
  ]
}
EOF
systemctl restart docker
```

（`mirror.ccs.tencentyun.com` 只在腾讯云机器上有效；阿里云用
`https://<你的ID>.mirror.aliyuncs.com`，控制台里能拿到专属地址。）

---

## 七、只想要一个"能给人看"的站点？

那就别买服务器。仓库里的 `docs/` 已经导出了纯静态演示站，
39 章正文、练习解析、讲解样例、星海图、术语表都能看，托管在 GitHub Pages 上：

<https://jscjscjscjscjsc.github.io/py-master/>

想要自己的域名，见本文档末尾"绑定自定义域名"一节。

**静态站和完整版的区别**：静态站没有后端，AI 答疑、账号进度、在线判题用不了。
让学生真做题，还是得上完整版。

---

## 八、绑定自定义域名（静态站）

1. Cloudflare 或阿里云买域名，DNS 加四条 A 记录指向 GitHub Pages：

   ```
   A  @  185.199.108.153
   A  @  185.199.109.153
   A  @  185.199.110.153
   A  @  185.199.111.153
   CNAME  www  jscjscjscjscjsc.github.io
   ```

2. 仓库 → Settings → Pages → Custom domain 填域名 → 勾 Enforce HTTPS
3. 把 `build_static_docs.py` 顶部的 `CNAME_DOMAIN` 填成域名，
   重新执行 `python build_static_docs.py`，会在 `docs/CNAME` 里带上

> Cloudflare 的橙色云朵（代理）建议先关掉，等 GitHub 签好证书再打开，
> 否则容易卡在证书校验。
