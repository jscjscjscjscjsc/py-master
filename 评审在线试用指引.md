# 评审在线试用：怎么把"测试链接"这条要求落地

> 对应大赛要求：**参赛教师应提供可供评审使用的教学智能体测试链接、二维码或
> 测试账号，并简要说明使用方法。如涉及学生数据、企业数据、校内平台数据等内容，
> 应进行脱敏处理。**
>
> 本文说明这件事的完整做法。材料填进申报表时，
> 直接用脚本生成的 `dist/review/评审访问说明.md`。

---

## 一、先说清楚：为什么静态站不够用

两个平台原来都只有 GitHub Pages 静态站。它能满足"看"，但满足不了这条要求里的
**"评审使用"**：

| | 静态站能做什么 | 这条要求实际考什么 |
|---|---|---|
| 浏览课程内容 | ✅ | — |
| **用星辰教练问一个问题** | ❌ 没有后端 | ✅ 智能体的核心能力 |
| **写代码 / 判题** | ❌ 要有 Python 执行环境 | ✅ 教学闭环 |
| **登录、看学习进度** | ❌ 要有数据存储 | ✅ 个性化 |
| 二维码指向的页面 | 能打开，但不能用 | 要能真的用起来 |

所以结论是：**必须有一个跑着后端的在线地址**。静态站可以继续留着做门面，
但填进"测试链接"栏的应该是能交互的那个。

---

## 二、方案：一台服务器，两个子站

```
                    ┌──────────────────────────────┐
   评审点链接 ──────▶│  Caddy（自动 HTTPS 证书）     │
                    └───────┬──────────────┬───────┘
                            │              │
              pymaster.域名 │              │ aimaster.域名
                            ▼              ▼
                    ┌─────────────┐  ┌──────────────┐
                    │  PyMaster   │  │  AI Master   │
                    │  Flask 容器 │  │  Flask 容器  │
                    └─────────────┘  └──────────────┘
                     演示账号/安全闸门   演示账号
```

- **配置**：香港/新加坡轻量服务器 2核4G（免备案、当天可用）
- **成本**：服务器 ¥40–70/月 + 域名 ¥60/年，比赛期间可按月付
- **时间**：买机器到链接可用，约 40 分钟

为什么放一台而不是两台：两个平台都很轻，共用一台省一半钱；
Caddy 一个实例签两张证书，也不需要额外配置。

---

## 三、满足"脱敏"要求的三个设计

这三个是这条要求里最容易做错的地方，都已落实到代码里。

### 1. 平台里本来就没有真实学生数据

已逐项核查：

| 检查项 | 结果 |
|---|---|
| 两个平台的模板、静态资源里有无姓名/学校/院系 | **零命中** |
| 题库里有无真实学生姓名 | **零命中**（`成绩单`是题目示例，如"f-string 格式化一张成绩单"） |
| 仓库里有无成绩单、登记册等原始教学文档 | **没有**，它们只在你本机 `Desktop/数据文件/` |

### 2. 评审账号是虚拟的，且不写盘

`demo_mode.py`（两个平台各一份）为评审准备了一个**预置学习痕迹的演示账号**：
有进度、积分、错题、修为等级 —— 评审打开就能看到"用起来之后"的样子，
而不是一个空壳。

关键在于：**他的一切操作只存在服务器内存里，一个字节都不写盘。**

实测（真实 HTTP 请求）：

```
登录演示账号        → 成功，修为 186、8 个已完成知识点、1 条错题
做一次操作          → 内存里生效
检查磁盘 users.json → 账号列表为空，没有 demo 字样
错误密码            → 仍然被拒（没有因为是"演示"而放宽）
```

这样评审可以随便点、随便改，你的真实教学数据不会被污染；
容器重启即回到初始状态。

### 3. 在线执行代码有安全边界

PyMaster 的判题是**把学生代码在服务器上真跑**（没有虚拟机隔离）。
本地版这样最省事，但挂到公网就等于把机器借人用。

`sandbox_guard.py` 做了两层：

- **静态检查**：拦掉起进程、网络请求、文件写入、动态执行这些系统级操作
- **资源限制**：子进程限 CPU 15 秒、内存 512MB、进程数 64

重点是**不误伤教学代码** —— 27 项自测里，拦的是 `import subprocess`、
`os.system`、`socket`、`eval`、写文件；放行的是切片、`math`、`random`、
`input()`、只读打开文件、`list.remove`（这个名字撞了黑名单但不该拦）。

> 说明：这是"降低风险"不是"绝对安全"。真要大规模公开，
> 应当把判题放进一次性容器（见 `上线操作手册.md` 第三节）。
> 评审这种小范围场景，这个防护级别是合适的。

---

## 四、操作步骤

### 第 0 步：买机器与域名

1. 轻量应用服务器 → **香港 / 新加坡** → Ubuntu 22.04 → **2核4G**
2. 安全组放行 **80 与 443**
3. 买域名，加两条 A 记录都指向该服务器 IP：
   ```
   pymaster.你的域名   →  服务器 IP
   aimaster.你的域名   →  服务器 IP
   ```

### 第 1 步：一条命令部署

在 PyMaster 项目根目录：

```bash
# 先看会打包什么（不动服务器）
python tools/deploy_review.py --host root@你的服务器IP \
    --domain 你的域名 --dry-run

# 正式部署
python tools/deploy_review.py --host root@你的服务器IP --domain 你的域名
```

脚本会自动完成：打包两个平台 → 上传 → 生成随机评审口令 →
写好两平台的演示账号环境 → 构建并启动容器 →

**并生成 `dist/review/评审访问说明.md`**，里面有链接、账号、二维码、
建议试用路径，可以直接贴进申报表。

### 第 2 步：填模型密钥

不填也能打开，但 AI 答疑会不可用 —— 而对"教学智能体"来说，
AI 对话恰恰是评审最想看的部分，**这一步别省**。

```bash
ssh root@你的服务器IP
cd /opt/review

# PyMaster（追加三行）
vi pymaster.env
  PYMASTER_AI_BASE_URL=https://opencode.ai/zen/go/v1
  PYMASTER_AI_MODEL=deepseek-v4.1-flash
  ARK_API_KEY=你的密钥

# AI Master
vi aimaster.env
  STARLAB_AI_BASE_URL=https://opencode.ai/zen/go/v1
  STARLAB_AI_MODEL=deepseek-v4.1-flash
  ARK_API_KEY=你的密钥

docker compose up -d
```

### 第 3 步：自己先当一次评审

部署完务必自己走一遍（这五步都通过才算好）：

1. 手机流量打开链接（不用 WiFi）→ 能开
2. 登录演示账号 → 能看到预置进度，不是空的
3. 刷题中心做一道题 → 点运行能出结果
4. 星辰教练问一句话 → 有流式回答（这一步验证模型密钥）
5. 换个浏览器再登录 → 回到初始状态（验证"不落盘"）

---

## 五、填进申报表时怎么写

申报表有"测试链接/二维码"栏时，直接写：

> 在线试用地址：
> - PyMaster 教学平台：https://pymaster.你的域名
>   测试账号 reviewer / 口令见附件说明
> - AI Master 星辰学习系统：https://aimaster.你的域名
>   测试账号 reviewer / 口令见附件说明
>
> 使用方法：打开链接后点击「登录」，用上述账号登录即可使用全部功能
> （AI 答疑、代码运行、学习进度、成长体系）。两平台均提供预置学习记录的
> 演示账号，评审期间的任何操作都不会保存，可放心试用。
> 详细试用路径见附件《评审访问说明》，二维码见附页。
>
> 数据说明：平台内不含任何真实学生数据；演示账号为评审专用虚拟档案，
> 操作不落盘；涉及学校与个人信息的内容已全部脱敏。

二维码用 `dist/review/` 下那两张 PNG，分别贴在对应平台的说明旁边。

---

## 六、常见问题

**Q：评审期间服务器要一直开着吗？**
要。建议比赛材料提交后保持到评审结束（一般 1–2 个月），按月付服务器费用即可。

**Q：口令被别人知道了会怎样？**
演示账号是内存态、不写盘，最坏情况是别人也去点着玩，不会动到你的数据。
但注意：**AI 答疑会消耗你配置的模型额度**，所以口令只在评审材料里给出，
不要发到公开渠道。

**Q：能不能只用 GitHub Pages 加个二维码交差？**
技术上能交，但评审点进去发现"只能看不能用"，会直接影响"智能与融合"
和"实施与效果"两项评分 —— 而这两项正是这份材料想拿分的地方。

**Q：提交后还能改链接吗？**
大赛原则上提交后不得修改。所以**部署好、自己验证通过之后再提交**，
别把没验过的链接写进去。
