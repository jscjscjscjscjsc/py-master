{"success": true, "question": {"id": "ch08-02", "title": "用 json 模块做配置的存取", "track": "course", "chapter_id": 8, "chapter_title": "模块、包与工程化", "topic": "", "difficulty": 2, "difficulty_label": "中等", "tags": ["json", "序列化", "文件"], "statement": "把字典与 JSON 字符串互相转换（本题不落盘，只做字符串层面的转换）：\n\n1. 已知 `config = {\"name\": \"PyMaster\", \"version\": 2, \"debug\": True, \"tags\": [\"python\", \"ai\"]}`，用 `json.dumps` 转成字符串 `text`（要求 `ensure_ascii=False`，中文不转义）\n2. 用 `json.loads` 把 `text` 转回字典 `restored`\n3. 断言 `restored == config`，把结果存进 `same`（布尔值）\n4. 打印 `text` 和 `same`", "starter_code": "import json\nconfig = {\"name\": \"PyMaster\", \"version\": 2, \"debug\": True, \"tags\": [\"python\", \"ai\"]}\ntext = \n", "hints": ["json.dumps(d, ensure_ascii=False) 才能保留中文", "转回来用 json.loads"], "expected_output": "{\"name\": \"PyMaster\", \"version\": 2, \"debug\": true, \"tags\": [\"python\", \"ai\"]}\nTrue", "needs_input": false, "attempts": 0, "runs": 0, "solved": false, "stars": 0, "wrong": 0, "last_error": "", "solution": "import json\nconfig = {'name': 'PyMaster', 'version': 2, 'debug': True, 'tags': ['python', 'ai']}\ntext = json.dumps(config, ensure_ascii=False)\nrestored = json.loads(text)\nsame = restored == config\nprint(text)\nprint(same)\n", "explanation": "`json.dumps` 是「Python 对象 → JSON 字符串」，`json.loads` 是反方向。记忆方法：**带 s 的处理字符串（string），不带 s 的处理文件（file）**，所以还有 `json.dump` / `json.load`。\n\n`ensure_ascii=False` 很关键：默认值 True 会把中文转成 `\\u4e2d\\u6587`，存配置文件时人根本没法读。中文项目里几乎总要加上这个参数。\n\n类型映射要注意：Python 的 `True` 变 JSON 的 `true`，字典的键会被强制转成字符串，元组会变成数组（转回来是列表，不是元组）。", "checks": ["assert isinstance(text, str) and json.loads(text) == config, 'text 应该是可被 json.loads 还原的字符串'", "assert 'PyMaster' in text, 'text 里应包含内容'", "assert restored == config, 'restored 应等于原字典'", "assert same is True, 'same 应为 True'", "assert _out.strip().endswith('True'), '最后应打印 True'"], "stdin": ""}}