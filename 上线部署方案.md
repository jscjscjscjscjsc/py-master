# PyMaster 上线方案

> 写给要掏钱买域名、租服务器的人：先说清"上线"其实分两件事，
> 再给一条今天就能做完的路，和一条带完整功能的路。

---

## 一、先分清两件事，别一上来就买服务器

| | 在线演示站（静态） | 完整平台（Flask） |
|---|---|---|
| 内容 | 39 章正文、练习与解析、讲解样例、星海图、术语表 | 全部内容 + AI 答疑 + 账号进度 + 在线判题 + 代码运行 |
| 靠什么跑 | 纯静态文件 | Python 后端 + 数据库文件 |
| 上线成本 | **¥0**（GitHub Pages 已经在上） | 域名 ¥60/年 + 服务器 ¥25–60/月 |
| 什么时候需要 | 给别人看、给学生预习、搜索引擎收录 | 学生真的要在线做题、要用 AI 老师 |

**现在已经上线的是第一种**：<https://jscjscjscjscjsc.github.io/py-master/>

---

## 二、⚠️ 上线完整版之前必须知道的结论

**判题和"运行代码"是把学生提交的 Python 直接在服务器上跑，没有沙箱。**
（见 `HANDOFF.md`：`判题与代码运行都是子进程无沙箱执行`）

这意味着一旦对公网开放，任何人都能在你的服务器上执行任意代码：
挖矿、删数据、拿它当跳板攻击别人。这不是"概率问题"，是被扫到就必然发生。

所以只有三条路，请选一条：

1. **公网只放静态演示站，完整版发给学生本地跑**（当前策略，最稳）。
   分发压缩包已经做到"解压 → 双击 → 能用"，不需要学生装任何东西。
2. **只给自己/小班用**：完整版上线，但套一层访问口令（本文方案 B 已内置），
   并且不要在公开渠道发链接。
3. **真要公开**：必须把判题放进一次性容器里跑（Docker + 只读根文件系统 +
   禁止出网 + CPU/内存/时间上限）。这是另一件工程，别用现在的代码直接上。

---

## 三、方案 A：只上线演示站（推荐先做，30 分钟，成本≈域名钱）

演示站已经好了，这一步只是给它一个自己的域名。

### 1. 买域名

| 渠道 | 价格 | 说明 |
|---|---|---|
| **Cloudflare Registrar** | 按成本价，`.com` 约 $10/年 | 不赚差价、免费 DNS、免费 CDN，**推荐** |
| 阿里云 / 腾讯云 | `.top`/`.xyz` 首年 ¥9–35 | 国内访问快，但要实名；用国内服务器还得备案 |
| Namecheap / Porkbun | `.com` 约 $10–12/年 | 老牌，支持支付宝的少 |

建议：**Cloudflare 买 + Cloudflare 托管 DNS**，一步到位，后面换服务器只改一条记录。

### 2. 把域名指向 GitHub Pages

在 Cloudflare 的 DNS 里加四条 A 记录（GitHub Pages 的 IP）：

```
类型 A   名称 @     值 185.199.108.153
类型 A   名称 @     值 185.199.109.153
类型 A   名称 @     值 185.199.110.153
类型 A   名称 @     值 185.199.111.153
类型 CNAME 名称 www  值 jscjscjscjscjsc.github.io
```

⚠️ Cloudflare 的橙色云朵（代理）**建议先关掉**（改成灰云 / DNS only），
等 GitHub 签好证书再打开，否则容易卡在证书校验。

### 3. 在仓库里告诉 GitHub 这个域名

1. 仓库 → Settings → Pages → Custom domain 填你的域名 → Save
2. 等它提示 DNS check successful，勾上 **Enforce HTTPS**
3. 回到仓库，新建一个内容就是域名一行的 `CNAME` 文件放在 **docs/** 目录下

   （Pages 的发布目录是 `docs/`，所以 CNAME 必须放 `docs/CNAME`，
   不是仓库根目录）

   我已经把它加进导出脚本里了，见 `build_static_docs.py` 的 `CNAME_DOMAIN`，
   填上域名后每次导出都会自动带上。

### 4. 验证

打开 `https://你的域名/`，首页应该出现"在线演示站"说明条，
点任意章节能进正文，讲解样例有图有声。

**成本：每年六七十块钱，服务器零成本。**

---

## 四、方案 B：演示站 + 完整平台（要 AI / 判题）

### 1. 选服务器

| 类型 | 典型价格 | 优点 | 缺点 |
|---|---|---|---|
| 国外 VPS（RackNerd / Vultr / DigitalOcean） | $20–60/年 起 | **免备案**，当天可用 | 国内访问慢一点（100–200ms） |
| 国内轻量服务器（阿里云/腾讯云） | ¥25–60/月 | 国内访问快 | **必须备案**，7–20 天 |
| PaaS（Render / Railway / Fly.io） | 免费档会休眠 | 不用管系统 | 免费档 CPU/内存带不动 pandas，且仍是裸奔判题 |

**最快路径：国外 VPS + 免备案域名。** 2 核 2G 起步（判题要 fork 子进程，
1G 内存跑第 26 章的数据分析题会 OOM）。

⚠️ 如果选国内服务器，域名必须先备案；备案期间可以用 IP + 端口先测。

### 2. 服务器上要做的（复制粘贴即可）

```bash
# 装 Docker
curl -fsSL https://get.docker.com | sh

# 拉代码（国内服务器直连 GitHub 慢时，用镜像地址）
git clone https://github.com/jscjscjscjscjsc/py-master.git
cd py-master

# 准备口令与域名
cp deploy/.env.example deploy/.env
docker run --rm caddy:2 caddy hash-password --plaintext '你的密码'   # 把输出填进 BASIC_HASH
vi deploy/.env      # 填 DOMAIN / BASIC_USER / BASIC_HASH

# 填模型密钥
cp .env.example .env
vi .env             # 填 PYMASTER_AI_BASE_URL / PYMASTER_AI_MODEL / ARK_API_KEY

# 起服务（Caddy 会自动申请 HTTPS 证书）
docker compose -f deploy/docker-compose.yml up -d --build
```

打开 `https://你的域名` → 输入口令 → 就是完整平台。

### 3. 这套配置里已经处理好的坑

- **SSE 流式不能缓冲**：`Caddyfile` 里 `flush_interval -1`，否则 AI 答疑要等
  整段生成完才一次性显示
- **gunicorn 要给线程**：`--threads 8`，单线程会被流式请求卡死
- **判题子进程**：镜像里用非 root 用户跑，`mem_limit` 限制内存
- **数据落盘**：`data/` 挂到宿主机，容器重建不丢账号与进度
- **端口冲突**：应用只用 5000，外部只暴露 Caddy 的 80/443

### 4. 上线后必做

```bash
# 看日志
docker compose -f deploy/docker-compose.yml logs -f pymaster

# 备份（账号、进度、题库记录都在这里）
tar czf pymaster-backup-$(date +%F).tar.gz deploy/data deploy/env
```

---

## 五、备案这件事（只有国内服务器需要）

- 域名实名：阿里云/腾讯云买完当天，上传身份证，1–2 天
- 网站备案：填主体信息 + 服务器信息，管局审核 7–20 天
- 没备案用国内服务器的 80/443 端口 = 会被拦

如果只是想快，**别碰国内服务器**，用国外 VPS 或干脆只上演示站。

---

## 六、我的建议（按性价比排序）

1. **今天就做**：买个域名绑到 GitHub Pages（方案 A，¥60/年）。
   学生、家长、面试官点开就能看 39 章内容，这一条几乎没有成本。
2. **教学场景**：让学生下载分发包本地跑完整版（已做到一键启动、离线可用）。
   教室里几十个人同时连一台服务器跑判题，体验不会好。
3. **真要公网完整版**：国外 VPS + 上面的 compose 配置 + 口令，
   并且先想清楚第三节的安全问题。

---

## 七、演示站用自定义域名前，记得改一处

`build_static_docs.py` 顶部的 `CNAME_DOMAIN = ''` 填成你的域名，
重新执行 `python build_static_docs.py` 就会生成 `docs/CNAME`。
不改的话 GitHub Pages 会继续用默认的 `github.io` 地址（不影响访问）。
