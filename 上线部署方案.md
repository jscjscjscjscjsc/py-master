# 上线部署方案（已合并到《上线操作手册》）

> **这份文档的内容已经并入 [上线操作手册.md](上线操作手册.md)，请直接看那一份。**
>
> 保留这个文件只是为了让旧链接不失效。下面是历史版本里仍然成立的两条结论，
> 细节与操作步骤都以上线操作手册为准。

## 结论没变的两条

**一、上线分两件事，别一上来就买服务器。**

- **纯静态演示站**：39 章正文、练习解析、讲解样例、星海图、术语表。
  跑在 GitHub Pages 上，成本 ¥0，现在就能看：
  <https://jscjscjscjscjsc.github.io/py-master/>
- **完整平台**：静态站的全部内容 + AI 答疑 + 账号进度 + 在线判题 + 代码运行。
  需要一台服务器，¥600/年 上下（30 人以内）。

**二、⚠️ 上线完整平台之前必须知道的结论。**

**判题和"运行代码"是把学生提交的 Python 直接在服务器上跑，没有沙箱。**
（见 `HANDOFF.md`：`判题与代码运行都是子进程无沙箱执行`）

一旦对公网开放，任何人都能在你的服务器上执行任意代码：挖矿、删数据、
拿它当跳板攻击别人。这不是"概率问题"，是被扫到就必然发生。

所以只有三条路：

1. **公网只放静态演示站，完整版发给学生本地跑**（成本最低，最稳）。
   分发压缩包已经做到"解压 → 双击 → 能用"，不需要学生装任何东西。
2. **只给自己/小班用**：完整版上线，但套一层访问口令，
   并且不要在公开渠道发链接。→ 30 人班的推荐做法，见操作手册第四节。
3. **真要完全公开**：必须把判题放进一次性容器里跑（Docker + 只读根文件系统 +
   禁止出网 + CPU/内存/时间上限）。这是另一件工程。
   折中做法是设 `PYMASTER_ALLOW_CODE_EXEC=0` 直接关掉在线运行代码，
   课程正文、练习解析、AI 答疑照常，只是动手练习要改用本地版。
